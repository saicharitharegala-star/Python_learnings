# Database Operations Project

A simple Python script that connects to PostgreSQL, creates a table, inserts data, and reads records.

## How to use

1. Activate your virtual environment.
2. Run `python test.py`.
3. Enter the requested values when prompted.

## Notes

- The script uses `psycopg2`.
- Make sure PostgreSQL is running and the connection details in `test.py` are correct.
