# Web Scraping Project

This project scrapes product details from a website.

It shows the product title, price and image URL. It also downloads the image and checks the price with the target price.

## Run the project

Open the folder and run:

```bash
python web_scraper.py
```

The images will be saved in the `downloaded_images` folder.
