# Flask Registration Form

## Run the application

```bash
python3 -m pip install -r requirements.txt
python3 app.py
```

Open `http://127.0.0.1:5000` in a browser. Enter a name, city, and phone number, then select **Register** to view the confirmation page.
